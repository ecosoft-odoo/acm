<odoo>
    <record id="view_res_users_role_form" model="ir.ui.view">
        <field name="name">res.users.role.form</field>
        <field name="model">res.users.role</field>
        <field name="inherit_id" ref="base_user_role.view_res_users_role_form"/>
        <field name="arch" type="xml">
            <field name="group_id" position="after">
                <field name="is_readonly_user"/>
                <field name="unrestrict_model_update"/>
            </field>
            <page name="role_inernal_notes" position="before">
                <page name="except_readonly_model" string="Except Readonly Model" attrs="{'invisible': [('is_readonly_user', '=', False)]}">
                    <field name="except_readonly_model_ids"/>
                </page>
            </page>
        </field>
    </record>
</odoo>
